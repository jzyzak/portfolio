export default function Loading() {
  return (
    <div className="paper-grid flex min-h-[50vh] items-center justify-center">
      <div className="reveal border border-[var(--border)] px-4 py-2 text-sm text-[var(--muted)]">Loading...</div>
    </div>
  );
}
